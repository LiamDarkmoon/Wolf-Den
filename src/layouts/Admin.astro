---
import Layout from './Layout.astro';
import { createClient } from "../db/supabase";

const path = Astro.url.pathname;

---
<Layout>
    <h1 class="text-2xl text-center font-bold border-b-2 border-primary text-primary mb-4 pb-4">
        <i class="fa-solid fa-layer-group me-1"></i>
        Panel de administracion
    </h1>
    <section class="h-full flex flex-col md:flex-row gap-4">
       
        <div class="md:w-1/4 flex md:flex-col gap-4 py-4 md:border-e-2 border-primary ">
            <a 
                class={path === "/admin/adventures" ? "w-full text-center md:text-start mb-2 p-2 rpunded-sm border-b border-primary/40 bg-primary/20" : "w-full text-center md:text-start mb-2 p-2 rounded-sm border-b group border-primary/40"}
                href="/admin/adventures"
            >
            <i class={path === "/admin/adventures" ? "fa-solid fa-gear me-2 text-primary" : "fa-solid fa-gear me-2 group-hover:text-primary"}></i>
                Aventuras
            </a>
            <a 
                class={path === "/admin/players" ? "w-full text-center md:text-start mb-2 p-2 rpunded-sm border-b border-primary/40 bg-primary/20" : "w-full text-center md:text-start mb-2 p-2 rounded-sm border-b group border-primary/40"}
                href="/admin/players"
            >
            <i class={path === "/admin/players" ? "fa-solid fa-gear me-2 text-primary" : "fa-solid fa-gear me-2 group-hover:text-primary"}></i>
                Jugadores
            </a>
            {
                path !== "/admin/" && (
                    <a 
                        class={"w-full text-center md:text-start mb-2 p-2 rounded-sm border-b border-primary/40 hover:text-primary"}
                        href="/admin/"
                    >
                        <i class="fa-solid fa-house me-2"></i>
                        Dashboard
                    </a>
                )
            }
        </div>
        <slot/>

    </section>
</Layout>

<style>

</style>